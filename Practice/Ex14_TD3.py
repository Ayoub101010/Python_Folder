"""Exercice 14
Soit la liste de nombres [8, 3, 12.5, 45, 25.5, 52, 1]. Triez les nombres de cette
liste par ordre croissant, sans utiliser la fonction sort().
Les fonctions et méthodes min(), .append() et .remove() vous seront utiles"""
#Tri_Par_selection
T = [8, 3, 12.5, 45, 25.5, 52, 1 ]

min_val = min(T) # On prend le premier élément comme valeur minimale
min=f"La valeur minimale est : {min_val}"
print(min)
for i in range(6):
    if T[i]<min_val:
     T[i]=min_val
for i in range(6):

    print(T)
    
    while len(liste)>0
      
     

    

