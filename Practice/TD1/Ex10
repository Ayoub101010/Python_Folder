
import math 
def SommeDesCarres(N): 
    S=0 
    for i in range (1, N+1): 
        S += pow(i,2) 
    print(f"La somme des carrés de 1 à {N} est : {S}")
       
M = int(input("veuillez saisir un nombre : "))
SommeDesCarres(M)

 